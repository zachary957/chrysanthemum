POJO&RPC方法的返回值和参数必须使用包装数据类型
局部变量推荐使用基本数据类型
原因:1.数据库可能为null导致NPE
     2.调用不成功会返回默认值比如0%,这是不合理的,所以包装类的null能表示额外信息

#{}在预处理时，会把参数部分用一个占位符 ? 代替
而${}则只是简单的字符串替换
优先使用#{}
原因:${}会导致sql注入问题

不得使用外键与级联，一切外键概念必须在应用层解决
原因:性能 并发 扩展性 技术问题